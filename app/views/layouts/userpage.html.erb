<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <title><%= content_for?(:title) ? yield(:title) : "bucket" %></title>

  <%= stylesheet_link_tag    "userpage" %>
  <%= javascript_include_tag "vendor/modernizr" %>
  <%= csrf_meta_tags %>
</head>

<body>
  <nav id="top-nav">
    <%= link_to :Home, :userpage_index %>

    <% if pass_signed_in? %>
    Welcome <%= current_pass.user.fullname %>
    <%= link_to "User Profile" %>
    <%= link_to :logout, :logout %>
    <% end %>
  </nav>
  <div id="left-nav">
    <div>
      <%= link_to "New goal", :goal_new  %>
      <%= link_to "All goals"%>
      <%= link_to "Favourited"%>
      <%= link_to "Done"%>
    </div>
  </div>
  <div id="right-content">
  <%= yield %>
  </div>
  <%= javascript_include_tag "userpage" %>
</body>
</html>